table:
  name: users
  schema: public
array_relationships:
  - name: book_loans
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: book_loans
          schema: public
  - name: user_books
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_books
          schema: public
