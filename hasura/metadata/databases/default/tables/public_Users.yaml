table:
  name: users
  schema: public
array_relationships:
  - name: book_loans
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: book_loans
          schema: public
  - name: user_books
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_books
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - name
        - email
      filter: {}
      allow_aggregations: true
insert_permissions:
  - role: anonymous
    permission:
      check: {}
      columns:
        - name
        - email
delete_permissions:
  - role: anonymous
    permission:
      filter: {}
