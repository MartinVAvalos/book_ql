table:
  name: book_inventory
  schema: public
object_relationships:
  - name: book
    using:
      foreign_key_constraint_on: book_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - book_id
        - quantity
      filter: {}
      allow_aggregations: true
