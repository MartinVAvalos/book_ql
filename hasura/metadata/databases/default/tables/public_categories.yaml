table:
  name: categories
  schema: public
array_relationships:
  - name: book_categories
    using:
      foreign_key_constraint_on:
        column: category_id
        table:
          name: book_categories
          schema: public
select_permissions:
  - role: anonymous
    permission:
      columns:
        - id
        - name
        - comment
      filter: {}
      allow_aggregations: true
